#include<vector>


using namespace std;


template <typename T>
void MergeMatrix(vector<T>& mass, size_t first, size_t last, size_t middle)
{

}


template <typename T>
vector <T> MergeSort(vector<T> mass)
{
	return mass;
}